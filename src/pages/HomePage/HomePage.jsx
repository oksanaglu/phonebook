import { Home, Title } from "./HomePage.styled";

export const HomePage = () => {
    return (
        <Home>
            <Title>Welcome to the Phonebook</Title>
        </Home>
    );
};

export default HomePage;